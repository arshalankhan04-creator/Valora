// Stub for sending email using Nodemailer
const sendEmail = async (options) => {
  // To be implemented in the notification phase
  console.log(`Email stub called with:`, options);
};

module.exports = sendEmail;
